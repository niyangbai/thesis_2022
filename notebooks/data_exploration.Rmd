---
title: "Data Exploration"
output: html_notebook
---
```{r}
rm(list = ls())
library(usmap)
library(ggplot2)
library(wagegap22package)
```


```{r}
load("D:/github/wagegap22/data/cleaned_data.RData")
head(df[c("ID", "state", "county", "wage_gap_B_W")])
```
```{r}
plot_usmap(data = df[which(df$year=='2017'),], values = "wage_gap_B_W", color = "black") +
  scale_fill_continuous(
    low = "green", high = "red", name = "Wage Gap between White and Black (2017)", label = scales::comma
  ) + theme(legend.position = "top")
```